#!/bin/bash
# Scale the Django app

echo "📈 Scaling deployment to 3 replicas..."
kubectl scale deployment messaging-deployment --replicas=3

echo "🧩 Verifying pods..."
kubectl get pods

echo "⚡ Load testing with wrk..."
wrk -t2 -c100 -d10s http://localhost:8000/

echo "📊 Resource usage:"
kubectl top pods
